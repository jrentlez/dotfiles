[core]
pager = delta --diff-highlight

[user]
email = jonas.zeltner@posteo.de
name = Jonas Zeltner
signingkey = ~/.ssh/id_ed25519.pub

[init]
defaultBranch = main

[merge]
conflictstyle = zdiff3

[diff]
minimal = true
mnemonicPrefix = true

[interactive]
diffFilter = delta --diff-highlight --color-only

[fetch]
prune = true

[gpg]
format = ssh

[gpg "ssh"]
allowedSignersFile = ~/.ssh/allowed_signers

[commit]
gpgsign = true
verbose = true

[tag]
gpgsign = true

[rebase]
missingCommitsCheck = warn

[log]
abbrevCommit = true

[delta]
keep-plus-minus-markers = true
file-style = "bold normal"
navigate = true
navigate-regex = "^(commit|added:|removed:|renamed:|Δ|•|(@@ -\\d+,\\d+ \\+\\d+,\\d+ @@))"

; vim: filetype=gitconfig
